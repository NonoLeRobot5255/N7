project TP2 is

   for Main use ("comprendre_mode_parametre.adb", "premier_programme.adb", "puissance.adb", "dates.adb", "specifier_et_tester.adb");

   package Builder is
      for Default_Switches ("ada") use ("-s");
   end Builder;

   package Compiler is
      for Default_Switches ("ada") use ("-gnata", "-gnatawa", "-g");
   end Compiler;

   package Binder is
      for Default_Switches ("ada") use ("-E");
   end Binder;

end TP2;

